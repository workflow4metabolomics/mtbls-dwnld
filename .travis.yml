notifications:
 email:
  recipients:
   - pierrick.rogermele@icloud.com

before_install:
    - echo "deb http://cran.univ-paris1.fr/bin/linux/ubuntu precise/" | sudo tee -a /etc/apt/sources.list
    - sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys E084DAB9
    - sudo apt-get update
    - sudo apt-get install -y r-base
    - sudo apt-get install -y make
    - sudo apt-get install -y g++
    - sudo apt-get install -y libxml2-dev
    - sudo apt-get install -y libcurl4-openssl-dev
    - sudo apt-get install -y libnetcdf-dev
    - sudo apt-get install -y gfortran
    - sudo apt-get install -y liblapack-dev
    - sudo apt-get install -y libblas-dev
    - sudo apt-get install -y libblas-dev
    - sudo R -e "install.packages(c('getopt', 'R.utils'), dependencies = TRUE, repos='http://lib.ugent.be/CRAN/')"
    - sudo R -e "source('http://bioconductor.org/biocLite.R') ; biocLite('Risa')"
    - wget http://download.asperasoft.com/download/sw/ascp-client/3.5.4/ascp-install-3.5.4.102989-linux-64.sh
    - sudo sh ascp-install-3.5.4.102989-linux-64.sh
    - sudo apt-get install -y python-virtualenv

script:
 - make test
