notifications:
 email:
  recipients:
   - pk.roger@icloud.com

install:
    - wget https://download.asperasoft.com/download/sw/cli/3.7.7/aspera-cli-3.7.7.608.927cce8-linux-64-release.sh
    - bash aspera-cli-3.7.7.608.927cce8-linux-64-release.sh
    - sudo apt-get install -y python-virtualenv

branches:
  only:
    - master
    - /^release[/].*$/

script:
    - echo $PATH
    - echo $HOME
    - export PATH=$PATH:$HOME/.aspera/cli/bin
    - echo $PATH
    - which ascp
    - make test
    - make planemolint
# Planemo test works locally, but not on Travis-CI
#    - make planemotest
